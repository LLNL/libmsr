The example gremlins are depend on the MPI profiling interface
and depend on the wrap.py script.  The script may be downloaded from
github using the following command:

git clone https://github.com/tgamblin/wrap.git

The makefiles expect wrap.py to be in the ../wrap directory.

